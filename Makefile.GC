.SUFFIXES:

ifeq ($(strip $(DEVKITPPC)),)
$(error "Please set DEVKITPPC in your environment. export DEVKITPPC=<path to>devkitPPC")
endif

include $(DEVKITPPC)/gamecube_rules

TARGET		:=	boot
BUILD		:=	build
SOURCES		:=	source source/engine/gc source/engine source/scenes source/objects source/tiles source/effects source/engine/lib/png/
DATA		:=	assets assets/audio assets/graphics
TEXTURES	:=	textures
INCLUDES	:=	source

CFLAGS		=	-g -O2 -Wall $(MACHDEP) $(INCLUDE)
CFLAGS      += -DCURRENT_PLATFORM=PLATFORM_$(PLATFORM)
CXXFLAGS	=	$(CFLAGS)
LDFLAGS		=	-g $(MACHDEP) -Wl,-Map,$(notdir $@).map

LIBS := -lfat
LIBS	+= -lm
LIBS	+= -lvorbisidec -logg -lasnd
LIBS	+= -logc

LIBDIRS	:= $(PORTLIBS)

ifneq ($(BUILD),$(notdir $(CURDIR)))

export OUTPUT	:=	$(CURDIR)/dist/gc/$(TARGET)
export VPATH	:=	$(foreach dir,$(SOURCES),$(CURDIR)/$(dir)) \
					$(foreach dir,$(DATA),$(CURDIR)/$(dir)) \
					$(foreach dir,$(TEXTURES),$(CURDIR)/$(dir))
export DEPSDIR	:=	$(CURDIR)/$(BUILD)

CFILES		:=	$(foreach dir,$(SOURCES),$(notdir $(wildcard $(dir)/*.c)))
CPPFILES	:=	$(foreach dir,$(SOURCES),$(notdir $(wildcard $(dir)/*.cpp)))
sFILES		:=	$(foreach dir,$(SOURCES),$(notdir $(wildcard $(dir)/*.s)))
SFILES		:=	$(foreach dir,$(SOURCES),$(notdir $(wildcard $(dir)/*.S)))
BINFILES	:=	$(foreach dir,$(DATA),$(notdir $(wildcard $(dir)/*.*)))
SCFFILES	:=	$(foreach dir,$(TEXTURES),$(notdir $(wildcard $(dir)/*.scf)))
TPLFILES	:=	$(SCFFILES:.scf=.tpl)

ifeq ($(strip $(CPPFILES)),)
	export LD	:=	$(CC)
else
	export LD	:=	$(CXX)
endif

export OFILES_BIN		:=	$(addsuffix .o,$(BINFILES)) $(addsuffix .o,$(TPLFILES))
export OFILES_SOURCES	:=	$(CPPFILES:.cpp=.o) $(CFILES:.c=.o) $(sFILES:.s=.o) $(SFILES:.S=.o)
export OFILES			:=	$(OFILES_BIN) $(OFILES_SOURCES)
export HFILES			:=	$(addsuffix .h,$(subst .,_,$(BINFILES))) $(addsuffix .h,$(subst .,_,$(TPLFILES)))

export INCLUDE	:=	$(foreach dir,$(INCLUDES),-iquote $(CURDIR)/$(dir)) \
					$(foreach dir,$(LIBDIRS),-I$(dir)/include) \
					-I$(CURDIR)/$(BUILD) \
					-I$(LIBOGC_INC)

export LIBPATHS	:= -L$(LIBOGC_LIB) $(foreach dir,$(LIBDIRS),-L$(dir)/lib)

.PHONY: $(BUILD) clean distclean

ifeq (,$(wildcard $(DEVKITPRO)/portlibs/ppc/include/tremor/ivorbiscodec.h))
$(BUILD):
	@echo "*---------------------------------------------------------*"
	@echo "Please install libvorbisidec:"
	@echo "(dkp-)pacman -S ppc-libvorbisidec"
	@echo "*---------------------------------------------------------*"
else
$(BUILD):
	@[ -d $@ ] || mkdir -p $@
	@mkdir -p $(CURDIR)/dist/gc
	@$(MAKE) --no-print-directory -C $(BUILD) -f $(CURDIR)/Makefile.GC PLATFORM=$(PLATFORM)
endif

clean:
	@echo clean ...
	@rm -fr $(BUILD)

distclean:
	@echo distclean ...
	@rm -f $(CURDIR)/dist/gc/$(TARGET).elf
	@rm -f $(CURDIR)/dist/gc/$(TARGET).dol

run:
	wiiload $(CURDIR)/dist/gc/$(TARGET).dol

else

DEPENDS	:=	$(OFILES:.o=.d)

$(OUTPUT).dol: $(OUTPUT).elf
$(OUTPUT).elf: $(OFILES)
$(OFILES_SOURCES) : $(HFILES)

%.jpg.o	:	%.jpg
	@echo $(notdir $<)
	$(bin2o)

%.png.o	:	%.png
	@echo $(notdir $<)
	$(bin2o)

%.tpl.o	%_tpl.h :	%.tpl
	@echo $(notdir $<)
	@$(bin2o)

%.ogg.o	%_ogg.h :	%.ogg
	@echo $(notdir $<)
	$(bin2o)

%.bin.o : %.bin
	@echo $(notdir $<)
	$(bin2o)

%.room.o : %.room
	@echo $(notdir $<)
	$(bin2o)

-include $(DEPENDS)

endif